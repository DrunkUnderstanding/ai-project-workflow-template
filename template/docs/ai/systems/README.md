# System Rules

Use this directory for technology- or domain-specific rules.

Examples:

- `unity-rules.md` for Unity projects.
- `web-rules.md` for web applications.
- `backend-rules.md` for backend services.
- `data-rules.md` for data pipelines.

Keep only rules that apply to the current project.

