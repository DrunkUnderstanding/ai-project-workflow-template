# CLAUDE.md

Read `AI.md` first.

Claude and Claude Code should follow the same shared project rules as every other AI assistant.

Do not duplicate long-lived project rules here. Update `docs/ai/` instead.

